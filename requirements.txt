# ========================================
# MI_AGENTE - Requirements Completos
# ========================================
# Para usar este proyecto desde cero, ejecuta:
# pip install -r requirements.txt

# Core - LangChain y OpenAI
langchain>=0.2.0
langgraph>=0.0.40
langchain-openai>=0.1.2
openai>=1.30.0
pydantic>=2.7

# Vector store / embeddings
faiss-cpu>=1.8.0
numpy>=1.25
tqdm>=4.66

# Document processing
PyMuPDF>=1.23.0  # Para fitz (PDFs)
python-docx>=1.1.0  # Para docx (Word)

# Utilities & env
python-dotenv>=1.0
httpx>=0.27
rich>=13.7
nest-asyncio>=1.5.0  # Para asyncio en Windows
typing-extensions>=4.8.0  # Para tipos avanzados

# LangSmith (tracing & debugging)
langsmith>=0.1.0

# Compatibilidad de zona horaria
backports.zoneinfo>=0.2.1  # Para Python < 3.9
tzdata>=2023.3  # Datos de zona horaria

# Optional (notebooks & widgets)
jupyterlab>=4.2
ipywidgets>=8.1

# Optional (type checking / lint)
ruff>=0.4
mypy>=1.10

# Dependencias adicionales para compatibilidad total
setuptools>=68.0.0
wheel>=0.41.0
pip>=23.0.0

# Para desarrollo (opcional)
black>=23.0.0  # Formateador de código
flake8>=6.0.0  # Linter
pytest>=7.0.0  # Testing